header.header
  nav.nav.nav--login
    a.nav__el(href='/') Home
  .header__logo
    img(src='#' alt='Natours logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        //- img.nav__user-img(src=`#` alt=`Photo of ${user.userName}`)
        span= user.userName.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='#') Sign up